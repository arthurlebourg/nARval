{
  "name": "narval",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp -r public/* dist/",
    "server": "tsc && vite build && cp -r public/* dist/ && node server",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.152.1",
    "bitecs": "^0.3.38",
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.17.0",
    "express": "^4.18.2",
    "path": "^0.12.7",
    "three": "^0.153.0",
    "ws": "^8.13.0"
  }
}
